import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    background: {
        backgroundColor: '#fff'
    },
    container: {
        marginHorizontal: '5%',
    }
})

export default style;