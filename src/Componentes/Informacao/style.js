import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    container: {
        marginTop: '20%',
        flex: 1,
        alignItems: 'center',
    },
    textAudio: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 51,
        letterSpacing: 0.65
    },
    textDescricao: {
        color: '#fff',
        fontSize: 14
    }
});

export default style;